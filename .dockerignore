.venv/
.git/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
*.egg-info/
tests/

# Secrets — never let these into a build context, even though nothing
# currently COPYs them; a future careless `COPY . .` shouldn't expose them
.env
.env.*
*.pem
*.key
credentials.json

# Not part of the running app — dead weight in the build context
CLAUDE.md
.claude/
docs/
steps-research/
PROJECT-CHECKLIST.md
PORTAL-BUILD-PLAN.md
TECH-STACK-BACKLOG.md
LEARNING-NOTES.md

# Generated fresh inside the build itself — never needed from the host
app/static/css/output.css

.DS_Store
